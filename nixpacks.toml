# Railway/Nixpacks: the app lives in server/, not the repo root.
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --omit=dev"]

[start]
cmd = "cd server && npm start"
